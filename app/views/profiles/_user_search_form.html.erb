<%= search_form_for q, url: url do |f| %>
  <div class="flex mb-4">
    <%= f.search_field :name_cont, placeholder: "ユーザー名で検索", class: "input input-bordered flex-1 w-full max-auto" %>
    <div class="flex-none ml-2">
      <%= f.submit class: 'btn btn-primary' %>
    </div>
  </div>
<% end %>